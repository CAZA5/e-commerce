<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>

        .card{
            width: 20%;
            height: 400px;
            border: solid 1px;
            border-radius: 5px;
            color: #34403A;
            border-color:#EA2793;
            margin-bottom: 1rem;
            margin-left: 1rem;
            display: inline-block;
    
        }

        .button{
            width: 150px;
            height: 50px;
            border-radius: 8px;
            display:block;
            margin-bottom:1rem;
        }

        .primary-button{
            background-color: #EA2793;
        }

        .secundary-button{
            background-color: #fff;
            border:solid 3px;
            border-color: #EA2793;
        }
    </style>
</head>
<body>
    <h1>Archivo index</h1>
    {{#each productosJson}}
        <div class="card">
            <p>{{nombre}}</p>
            <p>{{precio}}</p>
            <p>{{descripcion}}</p>
            <p>{{thumbnail}}</p>
            <p>{{sku}}</p>
            <p>{{stock}}</p>
            <p>{{prodcutoId}}</p>
            <button class="primary-button button">Eliminar</button>
        </div>
    {{/each}}


    <!---Sckets-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.7.5/socket.io.min.js"></script>

    <script src="../js/main.js"></script>
</body>
</html>